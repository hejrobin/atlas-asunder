import Container from 'data/items/Container';

const Knapsack: Container = new Container(
	'knapsack',
	'Canvas Knapsack',
	8,
	'An old green canvas knapsack, found it at a thrift store. Might be an old army knapsack?'
);

export default Knapsack;
